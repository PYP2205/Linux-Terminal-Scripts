#!/bin/bash
# Linux Shell script tht installs RPM files in the local directory
# Works in Red Hat or Red Hat Based systems (ex. Fedora) only.

# Installs all RPM files that are located in the direcotry the script is in
sudo rpm install *.rpm
